[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Single Page Application routing (if you are using react-router-dom)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
